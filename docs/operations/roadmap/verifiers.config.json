{
  "P0-01": {
    "type": "repo-scan",
    "description": "No direct fetch/axios in apps; SDK-only usage",
    "rules": [
      {
        "glob": "apps/**/*.{ts,tsx}",
        "disallowImport": ["axios", "node-fetch"],
        "message": "Direct HTTP client imports are forbidden in apps - use @digilist/client-sdk"
      },
      {
        "glob": "apps/**/*.{ts,tsx}",
        "disallowPattern": "\\bfetch\\s*\\(",
        "message": "Direct fetch() calls are forbidden in apps - use @digilist/client-sdk"
      },
      {
        "glob": "apps/**/*.{ts,tsx}",
        "requireImport": ["@digilist/client-sdk"],
        "minOccurrences": 1,
        "message": "Apps must use @digilist/client-sdk for all API calls"
      }
    ]
  },
  "P1-01": {
    "type": "playwright",
    "description": "SDK session continuity and auth refresh",
    "tests": [
      {
        "name": "session-persistence",
        "file": "tests/e2e/sdk/session-continuity.spec.ts",
        "description": "Session persists across page navigation and reload"
      },
      {
        "name": "token-refresh",
        "file": "tests/e2e/sdk/token-refresh.spec.ts",
        "description": "Token refresh occurs before expiry automatically"
      },
      {
        "name": "auth-error-recovery",
        "file": "tests/e2e/sdk/auth-error-recovery.spec.ts",
        "description": "Auth errors trigger re-authentication flow"
      }
    ]
  },
  "P5-01": {
    "type": "composite",
    "description": "Audit logging for all state mutations",
    "verifiers": [
      {
        "type": "repo-scan",
        "rules": [
          {
            "glob": "apps/api/src/**/*.{ts,tsx}",
            "requirePattern": "auditService\\.",
            "inFilesMatching": "**/*mutation*.ts",
            "message": "All mutation handlers must use auditService for logging"
          },
          {
            "glob": "packages/client-sdk/src/services/**/*.ts",
            "requirePattern": "(auditService|audit)",
            "message": "SDK services should integrate with audit logging"
          }
        ]
      },
      {
        "type": "playwright",
        "tests": [
          {
            "name": "audit-trail-creation",
            "file": "tests/e2e/audit/audit-trail.spec.ts",
            "description": "Mutations create audit trail entries with required fields"
          },
          {
            "name": "realtime-audit-events",
            "file": "tests/e2e/audit/realtime-events.spec.ts",
            "description": "WebSocket delivers realtime audit events"
          }
        ]
      }
    ],
    "requiredAuditFields": ["who", "what", "when", "tenantId", "ip", "ua"]
  }
}
