/**
 * @xalatechnologies/platform/ui/primitives
 *
 * Layer 1: Primitive UI components
 * Re-exported from @digdir/designsystemet-react
 *
 * These are the foundational building blocks for all UI:
 * - Layout components (Container, Grid, Stack)
 * - Form components (Button, Input, Select, Checkbox, Radio)
 * - Display components (Card, Badge, Avatar, Progress)
 * - Typography components (Heading, Paragraph, Label, Text)
 * - Feedback components (Alert, Toast, Dialog, Tooltip)
 * - Navigation components (Tabs, Breadcrumb, Link, Pagination)
 */

// Re-export Designsystemet primitives
export {
  // Layout
  // Container,
  // Grid,
  // Stack,

  // Forms
  Button,
  // Input,
  // Select,
  // Checkbox,
  // Radio,
  // Textarea,
  // Switch,

  // Display
  Card,
  // Badge,
  // Avatar,
  // Progress,
  // Spinner,

  // Typography
  Heading,
  Paragraph,
  // Label,

  // Feedback
  Alert,
  // Toast,
  // Dialog,
  // Tooltip,

  // Navigation
  // Tabs,
  // Breadcrumb,
  Link,
  // Pagination,
} from '@digdir/designsystemet-react';

// Re-export types
export type {
  ButtonProps,
  CardProps,
  HeadingProps,
  ParagraphProps,
  AlertProps,
  LinkProps,
} from '@digdir/designsystemet-react';
